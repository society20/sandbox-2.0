A database computer sample shipped with Play 2.0. We use MongoDB (instead of a relational database) and massive asynchronous actions with JQuery/Mustache.

For the dataset you have to run import-computerdb.bat before lauching the application.
